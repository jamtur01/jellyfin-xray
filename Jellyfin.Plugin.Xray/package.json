{
  "name": "jellyfin-plugin-xray-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild Web/src/index.js --bundle --format=iife --minify --loader:.css=text --outfile=Web/dist/xray.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "0.24.0",
    "jsdom": "25.0.1",
    "vitest": "3.2.6"
  }
}
